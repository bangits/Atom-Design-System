apiVersion: v1
kind: Service
metadata:
    name: "atomaks-2003"
    labels:
        app: "atomaks-2003"
spec:
    type: LoadBalancer
    ports:
    - port: 6000
      targetPort: 6000
      protocol: TCP
      name: http
    selector:
        app: "atomaks-2003"