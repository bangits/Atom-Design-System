import { ButtonProps } from '@my-ui/core';
import { FC } from 'react';
export interface PageWrapperProps {
    title: string;
    showButton?: boolean;
    buttonProps?: ButtonProps;
}
declare const _default: FC<PageWrapperProps>;
export default _default;
